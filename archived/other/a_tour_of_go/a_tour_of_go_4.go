package main
/*
import (
	"golang.org/x/tour/wc"
	"strings"
)

func WordCount(s string) map[string]int {
	maps := map[string]int{}
	words := strings.Split(s, " ")
	
	for _, v := range words {
		count := 0
		for _, v2 := range words {
			if v == v2 {
				count++
			}
		}
		maps[v] = count
	}
	return maps
}

func main() {
	wc.Test(WordCount)
}
*/